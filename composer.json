{
    "name": "f3rva/f3-api",
    "description": "API for all big data related functions",
    "autoload": {
        "psr-4": {
            "": "src/"
        }
    },
    "require": {
        "php-di/php-di": "^6.3",
        "doctrine/annotations": "^1.13"
    },
    "require-dev": {
        "phpunit/phpunit": "^9.5"
    },
    "scripts": {
        "post-install-cmd": [
            "# creating sqlite db folder",
            "mkdir -p db"
        ],
        "test": "phpunit --testdox test"
    }
}
